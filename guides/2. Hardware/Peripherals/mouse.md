# Mouse
A [Mouse](computer-mouse) is a [peripheral](peripheral) which allows **a user** to **control a computer** by moving a **pointer** (or **cursor**) on a display.

To interact with an specific element on the screen, its click button can be pressed.

Modern mice have a secondary click button to perform an alternate action, and a scroll wheel to scroll through content.
